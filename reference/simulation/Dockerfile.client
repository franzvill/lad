FROM python:3.12-slim

WORKDIR /app

# Install dependencies
COPY client/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# Copy client code
COPY client/ ./client/
COPY simulation/guest_device.py ./

# Run guest device simulation
CMD ["python", "guest_device.py"]
